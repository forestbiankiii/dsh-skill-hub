{
  "name": "dsh-skill-hub",
  "version": "0.1.0",
  "private": true,
  "description": "DSH dynamic Cordis plugin: list every resolved skill in Settings, toggle each one on or off, and read the shared ~/.agents/skills repository live.",
  "type": "module",
  "license": "MIT",
  "author": "forestbiankiii",
  "keywords": [
    "dsh",
    "deepseek-harness",
    "cordis",
    "plugin",
    "skills",
    "agent-skills"
  ],
  "scripts": {
    "check": "node --check src/host/host.js && node --check src/client/client.js && node --check deploy/compact.host.js && node --check deploy/compact.client.js && node --check tools/payload.mjs && node --check tools/build-payload.mjs && node --check tools/verify-payload.mjs",
    "test": "node tools/build-payload.mjs --check && node tools/verify-payload.mjs",
    "payload": "node tools/build-payload.mjs --write",
    "payload:compact": "node tools/build-payload.mjs --compact --json"
  },
  "engines": {
    "node": ">=20"
  }
}
